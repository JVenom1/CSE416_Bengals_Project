import React from 'react';
const ClusterDetailTable = ({ clusterDet }) => {
    // const keys = Object.keys(clusterDet);
    // averagedistancehd | averagedistanceot | numberofplans
    console.log(clusterDet)
    return (
        <></>
    );
}
export default ClusterDetailTable;
